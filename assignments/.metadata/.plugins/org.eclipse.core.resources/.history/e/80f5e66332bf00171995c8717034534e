package com.revature.streams;

public class fruityStreams {
	Double totalFruitPrice = fruitList.parallelStream().filter( car -> {
        if("subaru".equals(car.getMake())) {
            return true;
        }
        return false;
    })
    .map(car -> car.getPrice())
    .reduce((acc, cur) -> acc+cur).get();
}
